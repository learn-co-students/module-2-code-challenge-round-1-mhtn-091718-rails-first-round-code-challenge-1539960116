<h1>CREATE A NEW SUPERHEROINE  🙆‍♀️ </h1>

<% if flash[:error] %>
  <h2>There were the following errors with your submission: </h2>

  <% flash[:error].each do |error| %>
  <p><%= error %></p> 
  <% end %>
<% end %>

<%= form_for @heroine  do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :super_name %>
  <%= f.text_field :super_name %>
  <%= f.label :power %>
  <%= f.collection_select :power_id, @powers, :id, :name %>

  <%= f.submit %>
<% end %>
