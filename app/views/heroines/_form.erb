<% if flash[:error] %>
  <% flash[:error].each do |error| %>
  <%= error %>
  <% end %>
  <% end %>

  <%= form_for @heroine do %>
    <%= f.label :name %>
    <%= f.text_field :name%>
    <%= f.label :super_name %>
    <%= f.text_field :name%>
    <%= f.label :power %>
    <%= f.collection_select :power_id, Power.all, :id, :name%>
    <%= f.submit %>
    <% end %>
